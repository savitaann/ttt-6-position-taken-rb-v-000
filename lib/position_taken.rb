def position_taken?(board, index)

  end


def position_taken?(board, index)
  if board[0] != [" "]
    false
  end
end

def position_taken?(board, index)
  if board[0] != [""]
    false
  end
end

def position_taken?(board, index)
  if board[0] == ["X"] || board[0] == ["O"]

  end
end
