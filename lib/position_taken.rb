def position_taken?(board, index)
  end


def position_taken?(board, index)
  if board[index] == " "
    return false
      
  end
end

def position_taken?(board, index)
  if board[0] != [""]
    false
  end
end

def position_taken?(board, index)
  if board[0] == ["nil"]
    false
  end
end

def position_taken?(board, index)
  if board[0] == ["X"] || board[8] == ["O"]

  end
end
