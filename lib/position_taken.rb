def position_taken?(board, index)
  end


def position_taken?(board, index)
  if board[index] == " "
    return false
  end
end

def position_taken?(board, index)
  if board[index] == ""
    return false
  end
end

def position_taken?(board, index)
  if board[index] == nil
    return false
  end
end

def position_taken?(board, index)
  if board[index] == "X" || board[index] == "O"
      return true
  end
end
